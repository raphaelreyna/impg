CREATE TABLE IF NOT EXISTS title_principals (
       tconst VARCHAR(16),
       ordering INTEGER,
       nconst VARCHAR(16),
       category VARCHAR(64),
       job VARCHAR(256),
       characters VARCHAR(256)[]
);
